services:
  - type: web
    name: qrafty-api
    runtime: node
    rootDir: backend/api
    plan: starter
    buildCommand: npm ci --include=dev && npm run prisma:generate && npm run build
    preDeployCommand: npm run prisma:migrate:deploy
    startCommand: npm run start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: qrafty-db
          property: connectionString
      - key: SHADOW_DATABASE_URL
        fromDatabase:
          name: qrafty-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      # Set this in Render dashboard (or add here) to your deployed web app origin.
      # Example: https://<your-frontend-domain>
      # - key: WEB_ORIGIN
      # - key: GOOGLE_CLIENT_ID
      # - key: GOOGLE_CLIENT_SECRET
      # - key: GOOGLE_REDIRECT_URL

databases:
  - name: qrafty-db
    plan: basic-256mb
